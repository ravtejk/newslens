"""NewsLens — memory-threaded daily news briefing (personal prototype).

Keep this module import-side-effect free and stdlib-only: the doctor script
must be importable before `pip install` has ever run.
"""

__version__ = "0.1.0"  # milestone 1
